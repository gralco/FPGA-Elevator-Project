rule:
  # Allow default signal assignments
  signal_007:
    disable: true

  # Allow default variable assignments
  variable_007:
    disable: true

  # Set the max line length to 441 characters
  length_001 :
     length : 441
